{
  "name": "lerna-temp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "version": "lerna version --conventional-commits",
    "publish": "lerna publish --conventional-commits --skip-npm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
