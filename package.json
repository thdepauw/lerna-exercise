{
  "name": "lerna-temp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "version:master": "lerna version --conventional-commits",
    "version:dev": "lerna version --conventional-commits --conventional-prerelease",
    "publish:master": "lerna publish --conventional-commits --skip-npm",
    "publish:dev": "lerna publish --conventional-commits --conventional-prerelease --skip-npm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
